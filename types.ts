export interface Benefit {
  title: string;
  description: string;
}

export interface Module {
  title: string;
  days: string;
  goal: string;
  items: string[];
}

export interface FaqItem {
  question: string;
  answer: string;
}